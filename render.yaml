services:
  - type: web
    name: trail-scout-api
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: VOCAL_BRIDGE_API_URL
        value: https://api.vocalbridgeai.com
